time = input("What time is it? ")
hours, minutes = time.split(":")

hours = int(hours)
minutes = int(minutes)

def convert(time):
    float(minutes) * 100 / 60
    hours + minutes

if 7 <= hours <= 8:
    print("breakfast time")
